플래그 메모리 초기화

예문 코드 : bool flag[101][101] = { false };

전역 변수로 선언 시 모든 아이템이 기본적으로 false로 초기화되지만, 지역 변수로 선언할 경우 무작위 true/false로 지정되기 때문에 반드시 명시적으로 초기화를 해주어야 한다.

예문 코드를 통해 배열의 모든 아이템을 동일한 값으로 바로 초기화 할 수 있음.

특히 함수를 실행할 때마다 새로운 플래그 데이터를 사용해야할 경우 유용함.



* 2636 문제를 틀린 결정적 이유이므로 기억할 필요가 있음.